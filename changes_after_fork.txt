Renaming
  downloadfile.py ==> dowload_ephemeris.py
  lastned() ==> dowload_ephemeris()
  
New feature
  concurrency.py - conurrent prossesing for finding DOP Values and satellite count at point.

find_dop_on_point() - change input parameters. Concurrent processing using multiprocessing.pool demands args to be picklable.
                      Main difference was not passing "src" as an arg, but extracting necessary components and pass
                      those as args. This change also affected functions called by find_dop_on_point().

Remove DOPvalues_2 so only one DOPvalues is used.



Improvements:
* Finding a route is not limited to EV136.
* Routes work when overlapping roads, such as inside spiraling tunnels.
* Routes work over ferry crossings.
* Can find speedlimits without hardcoding it for EV136.
* Better documentation and overall concistency.
* Add config.
* Close the visual gap in frontend at the end of elevation-curve in the skyplot.
* More accurate terrain analysis along borders between .tif files.
* The backend can handle multiple (different) requests from multiple clients, but they are queued (only one is served at a time).


Future work:
* NVDB APIv3 is used inside function "get_road_and_speedlimits", must be upgraded to v4.
  The route finder in "get_road_and_speedlimits" often dont find routes, even easy short ones.
  maybe v4 is better.
* The option "Vegsystemreferanse" in the frontend is not used and should be removed.
* Potentially hard to see difference between zero DOP and
  close to zero DOP as it is not. Invalid DOP values (zeros) should be more obviouse.
  Maybe colorcode or set -1 instead of 0.
* DOP values should be set invalid inside tunnels.
* Conscider python library 'dask pandas' for sharing memory inside concurrency.py (bypass GIL) and make it even faster.
* Give system posibility to handle multiple different 'merged_raster' and daynumber rinex's, for different clients.
* The need to create new merged_raster for each request is suboptimal,memory heavy,
  and makes the backend voulnerable to buffer overflow attacks.
  The code should be refactored so that terrain analysis can be performed inplace in the dem10 data.
* Concider adding code to support GDOP calculation on future date, but set max limit.
  Satellites drift kilometers (exponentially) in a week (which is neglible at 20'000km distance).
  Set max to 14 or 30 days?.
  Allowing days in the future also solves that Norway is UTC+2, so finding DOP between kl 00-02 does not result in
  confusion (from missing RINEX file.)